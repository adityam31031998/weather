import React from "react";
import "../componentCss/SearchLocation.css";
const Temperature = ({ locationResponse }) => {
  return (
    <div className="days">
      <div className="currentDate"></div>
      <div className="currentDate"></div>
      <div className="currentDate"></div>
      <div className="currentDate"></div>
      <div className="currentDate"></div>
    </div>
  );
};

export default Temperature;
